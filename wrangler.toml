name = "aa218-workers"
main = "src/worker.js"
compatibility_date = "2024-10-01"

# Optional: test on workers.dev too
# workers_dev = true

[observability]
  head_sampling_rate = 1
  [observability.logs]
    enabled = true
    head_sampling_rate = 1
    invocation_logs = true
    persist = true
